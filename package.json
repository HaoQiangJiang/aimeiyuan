{
  "name": "our-little-universe-v3",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "db:local": "wrangler d1 migrations apply LOVE_DB --local",
    "db:remote": "wrangler d1 migrations apply LOVE_DB --remote",
    "photos:sync": "bash scripts/sync-photos.sh"
  },
  "dependencies": {
    "@cloudflare/vite-plugin": "latest",
    "@vitejs/plugin-react": "latest",
    "framer-motion": "^13.1.1",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest",
    "wrangler": "latest"
  }
}
